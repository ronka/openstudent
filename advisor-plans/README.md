# Implementation Plans (advisor)

Generated by the improve skill (`next` variant, scoped to `src/app/(tabs)/pomodoro.tsx`) on 2026-07-26, at commit `1b73daf`. This directory is `advisor-plans/` (not `plans/`) because `plans/` already holds the maintainer's own feature plan documents.

Session ran non-interactively, so plans were written for the top findings by leverage (the skill's default); the maintainer should review the findings table in the session summary and delete any plan they don't want before execution.

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Wall-clock timer + completion "ring" | P1 | M | — | DONE |
| 002  | Breaks, 4-round cycle, configurable focus duration | P1 | M | 001 | TODO |
| 003  | Session log + focus stats (screen + dashboard card) | P2 | M | 001 | TODO |
| 004  | Spike: Pomodoro Live Activity feasibility (expo-widgets, iOS) | P3 | M (spike) | — (feature would need 001) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 and 003 both extend `src/data/pomodoro-timer.ts`, created by 001. 001 must land first.
- 002 and 003 are independent of each other; 003 notes one interaction (only *focus* completions log a session once 002's phases exist — 002 Step 2.5 / 003 Step 2.1 both encode it), so if both run, execute 002 before 003 or re-check that rule.
- 004 can run anytime (read-only spike); the build plan it may spawn depends on 001.

## Verification baseline (recorded at `1b73daf`)

- `npx tsc --noEmit`: 5 pre-existing errors (`@/global.css` side-effect import in `src/constants/theme.ts`; `expo-widgets` types in 4 `src/widgets/*` files). Gate = no NEW errors.
- `npm run lint`: `✖ 18 problems (10 errors, 8 warnings)`, none in the pomodoro screen or `src/data/`. Gate = no problems in touched files.
- No test runner exists in the repo; plans use typecheck + lint + manual QA scripts.

## Findings considered and rejected / deferred

- **Attach a pomodoro to a specific task/assignment** (how-to step 1 says "decide on the task"; the app has assignments and quick-tasks): deferred — real product-design surface (picker UX, per-course attribution) with thin evidence of demand; revisit after 003 ships and per-course focus stats become the pull.
- **Sound/haptics via new deps (expo-audio / expo-haptics)**: not planned — the completion notification's default sound (plan 001) covers the "ring" without a new native dependency; revisit only if users report the notification sound is insufficient.
- **Auto-starting breaks/next round**: explicitly out of scope in 002 — manual start keeps the user in control and avoids surprise timers; the staged-phase model supports adding it later cheaply.
- **Streaks / weekly goals / focus charts**: deferred out of 003 (today/week/total only) — ship the loop, then decide if gamification fits the app's tone.
